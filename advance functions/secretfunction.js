var secret_keeper = funcion (string1, string2){
  var secret = "Johnny eats babies";
  var newString = string1 + secret + string2;
  console.log(newString);
}
//console.log(secret);

secret_keeper("keep it secret", "keep it safe");
